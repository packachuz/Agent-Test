{
  "name": "agent-test-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prebuild": "rm -rf public/dashboard && mkdir -p public && cp -r ../web public/dashboard",
    "predev": "npm run prebuild",
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0"
  }
}
