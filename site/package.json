{
  "name": "agent-test-site",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "prebuild": "rm -rf public/dashboard data && mkdir -p public/dashboard data/runs && node scripts/build-jsx.mjs && (cp ../runs/*.jsonl data/runs/ 2>/dev/null || true)",
    "predev": "npm run prebuild",
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@babel/core": "^7.25.0",
    "@babel/preset-react": "^7.25.0"
  }
}
